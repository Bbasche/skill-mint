import { Account, AccountId, ChainInfo, DatasetInfo } from '../core/network';
import { Transaction, TxReceipt } from '../core/tx';
import { Api } from './api';
import { ClientConfig } from './config';
import { GenericResponse } from '../core/resreq';
import { TxInfoReceipt } from '../core/txQuery';
import { CallClientResponse, Message } from '../core/message';
import { BroadcastSyncType, BytesEncodingStatus, EnvironmentType } from '../core/enums';
import { KGWAuthInfo, AuthSuccess, AuthenticatedBody, LogoutResponse } from '../core/auth';
import { HealthResponse, SelectQueryRequest } from '../core/jsonrpc';
import { HexString } from '../utils/types';
export default class Client extends Api {
    private unconfirmedNonce;
    private jsonRpcId;
    constructor(opts: ClientConfig);
    protected getAuthenticateClient(): Promise<GenericResponse<KGWAuthInfo>>;
    protected postAuthenticateClient<T extends EnvironmentType>(authBody: AuthenticatedBody<BytesEncodingStatus.HEX_ENCODED>): Promise<GenericResponse<AuthSuccess<T>>>;
    protected logoutClient<T extends EnvironmentType>(identifier?: Uint8Array): Promise<GenericResponse<LogoutResponse<T>>>;
    protected getAccountClient(accountId: AccountId): Promise<GenericResponse<Account>>;
    protected listDatabasesClient(owner?: Uint8Array): Promise<GenericResponse<DatasetInfo[]>>;
    protected estimateCostClient(tx: Transaction): Promise<GenericResponse<string>>;
    protected broadcastClient(tx: Transaction, broadcastSync?: BroadcastSyncType): Promise<GenericResponse<TxReceipt>>;
    protected pingClient(): Promise<GenericResponse<string>>;
    protected chainInfoClient(): Promise<GenericResponse<ChainInfo>>;
    protected healthModeCheckClient(): Promise<GenericResponse<HealthResponse>>;
    protected challengeClient(): Promise<GenericResponse<HexString>>;
    protected selectQueryClient(query: SelectQueryRequest): Promise<GenericResponse<Object[]>>;
    protected txInfoClient(tx_hash: string): Promise<GenericResponse<TxInfoReceipt>>;
    protected callClient(msg: Message): Promise<CallClientResponse<any>>;
    private buildJsonRpcRequest;
    private checkAuthError;
    private parseQueryResponse;
}
