import { ActionBody, CallBody } from '../core/action';
export declare const validateNamespace: (namespace: string) => boolean;
export declare const resolveNamespace: (actionBody: ActionBody | CallBody) => string;
